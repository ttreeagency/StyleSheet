prototype(Ttree.StyleSheet:External) < prototype(Fusion:Tag) {
    @context.uri = ${this.uri}
    tagName = 'link'
    selfClosingTag = TRUE
    attributes {
        rel = 'stylesheet'
        href = ${uri}
    }
}
