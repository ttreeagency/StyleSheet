prototype(Ttree.StyleSheet:External) < prototype(Neos.Fusion:Tag) {
    @context.uri = ${this.uri}
    tagName = 'link'
    selfClosingTag = TRUE
    attributes {
        rel = 'stylesheet'
        href = ${uri}
    }
}
