prototype(Ttree.StyleSheet:Resource) < prototype(TypoScript:Tag) {
    @context.uri = ${this.uri}
    tagName = 'link'
    selfClosingTag = TRUE
    attributes {
        rel = 'stylesheet'
        href = TypoScript:ResourceUri {
            path = ${uri}
        }
    }
}
